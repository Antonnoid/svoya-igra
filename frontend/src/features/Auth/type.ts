export type Registr = {
  name:string;
  email:string;
  password:string;
};

export type Login = {
  name:string;
  password:string;
};

export type User = {
  name:string;
  email:string;
  id:number;
  score:number;
};
